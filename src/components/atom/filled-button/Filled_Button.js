import React from 'react'

const Filled_Button = (props) => {
    return (
        <div className="filled-btn">
            <button>{props.title}</button>
        </div>
    )
}

export default Filled_Button
